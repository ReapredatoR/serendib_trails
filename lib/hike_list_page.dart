// hike_list_page.dart
class HikeListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("Hike List")),
        body: Center(child: Text("Hike List Content")));
  }
}
